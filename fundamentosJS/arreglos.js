const miObjeto = {
    miNumero: 7445555222,
    miMail: 'amilcar.asaa@gmail.com',
    miEdad: 18,    
}

const emptyArray = [1,2];
const arreglo = [1, 2, 'Hola', true, miObjeto];

//console.log(arreglo, emptyArray);

//Agregar elementos al arreglo
emptyArray.push('Amilcar');
console.log(emptyArray);


//Listas
//Una lista es practicamente un arreglo con la ligera diferencia que una lista debe tener datos del mismo tipo

const lista = [1,2,3,4,5,7];

console.log(lista);